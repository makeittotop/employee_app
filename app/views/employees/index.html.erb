
<!--
<div class="row">
  <div class="jumbotron">
    <h1 class="welcome-message">Welcome to the Employee Application!</h1>
  </div>
  <div id="custom-search-input">
    <div class="input-group col-md-12">
      <input type="text" class="  search-query form-control" placeholder="Search" />
      <span class="input-group-btn">
        <button class="btn" type="button">
          <span class=" glyphicon glyphicon-search"></span>
        </button>
      </span>
    </div>
  </div>
</div>
-->

<div class="row">
  <%= render 'layouts/sidebar' %>

  <div class="col-sm-9 col-sm-offset-3 col-md-10 col-md-offset-1 main">

    <!-- flash message --> 
    <%= render 'layouts/flash' %>

    <div class="row">
      <div class="bs-example">
        <% @employees.each do |employee| %>
          <div class="col-sm-6 col-md-3">
            <div class="thumbnail">
              <img data-src="holder.js/250x300" alt="Generic placeholder thumbnail">
              <div class="caption caption2">
                 <table class="employee-details">
                   <tbody class="employee-tbody">
                     <tr>
                       <td>
                          <strong><%= "#{employee.first_name} #{employee.last_name}".titleize %></strong>
                       </td>
                       <td>
                         <strong><%= employee.department %></strong>
                       </td>
                     </tr>
                     <tr>
                       <td>
                         <strong><%= employee.personal_email %></strong>
                       </td>
                       <td>
                         <strong><%= employee.contact_number %></strong>
                       </td>
                     </tr>
                   </tbody>
                 </table>
              </div>
              <div class="show-edit">
                <% unless session[:employee_id].blank? %>
                  <% if session[:admin] || session[:employee_id] == employee.id %>
                    <%= link_to 'Edit', edit_employee_path(employee), class: "btn btn-default btn-primary" %>
                  <% end %>
                <% end %>
                  <%= link_to 'Show', employee_path(employee.id), class: "btn btn-default btn-primary" %>
              </div>
            </div>
          </div>
        <% end %>
      </div><!-- /.bs-example -->
    </div>
  </div>

</div>

<div class="row">
<!--
<table>
  <thead>
    <tr>
      <th>First name</th>
      <th>Last name</th>
      <th>Date of birth</th>
      <th>Gender</th>
      <th>Country</th>
      <th>Personal email</th>
      <th>Phone number</th>
      <th>Employee code</th>
      <th>Department</th>
      <th>Status</th>
      <th>Hire</th>
      <th>End Hire</th>
      <th>Avatar</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @employees.each do |employee| %>
      <tr>
        <td><%= employee.first_name %></td>
        <td><%= employee.last_name %></td>
        <td><%= employee.date_of_birth %></td>
        <td><%= employee.gender %></td>
        <td><%= Carmen::Country.coded(employee.country).name %></td>
        <td><%= employee.personal_email %></td>
        <td><%= employee.contact_number %></td>
        <td><%= employee.code %></td>
        <td><%= employee.department %></td>
        <td><%= employee.status %></td>
        <td><%= employee.hire %></td>
        <td><%= employee.end_hire %></td>
        <td><%= image_tag employee.avatar_url(:thumb) if employee.avatar.present? %></td>

        <div class="btn-group btn-group-sm">
          <td><%= link_to 'Show', employee, class: "btn btn-default btn-primary" %></td>
          <td><%= link_to 'Edit', edit_employee_path(employee), class: "btn btn-default btn-info" %></td>
          <td><%= link_to 'Destroy', employee, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-default btn-danger" %></td>
        </div>

      </tr>
    <% end %>
  </tbody>
</table>

<br>
-->
<%= link_to 'New Employee', new_employee_path, class: "btn btn-info new-employee" %>
</div>

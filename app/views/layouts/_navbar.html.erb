<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand navbar-brand-row" href="#">Barajoun Studios</a>
      <ul class="nav navbar-nav navbar-right navbar-brand-row">
        <li><a href="#">Apps</a></li>
        <li><a href="#">Settings</a></li>
        <li><a href="#">About</a></li>
      </ul>
    </div>
   
    <div id="navbar" class="navbar-collapse collapse">
      <a class="navbar-brand-center" href="#">Employee Portal</a>

      <!-- form_tag RoR -->
      <% unless session[:employee_id]%>
          <%= form_tag logins_path, {method: "post", class: "navbar-form navbar-right"} do %>
            <%= text_field_tag :login, "", {class: "form-control login-form", placeholder: "Login"} %>
            <%= password_field_tag :password, "", {class: "form-control login-form", placeholder: "Password"} %>
            <%= submit_tag "Submit", class: "btn btn-primary" %>
            <%= %>
          <% end %>
      <% else %>
          <ul class="nav navbar-nav navbar-right">
              <li><%= link_to "Welcome #{session[:employee_first_name]}", "#", class: "login-name" %></li>
              <li><%= link_to "Logout", login_path(session[:employee_id]), method: "delete", class: "btn" %></li>
          </ul>
      <% end %>
      
      <!--
      <form class="navbar-form navbar-right">
        <input type="text" class="form-control login-form" placeholder="Login">
        <input type="text" class="form-control login-form" placeholder="Password">
        <input type="submit" value="Login" class="btn btn-info">
      </form>
      -->
      <form class="navbar-form navbar-right">
        <input type="text" class="form-control" placeholder="Search...">
      </form>
    </div>
  </div>
</nav>

